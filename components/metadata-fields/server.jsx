// import renderComponent from "../../packages/utils/render-component";
// import Component from "./Component";

export default async (args, info) => {
  /* const renderProps = {
    ...args,
  };

  return renderComponent({
    Component,
    componentName: "metadata-fields",
    args: renderProps,
  }); */
};
